# Распознавание эмоций по лицу 

Проект для распознавания эмоций по фото/видео с использованием ResNet18. (Также описано [обоснование выбора данной модели как итоговой](docs/testing_different_models.md))
Включает обучение, инференс через вебку, Streamlit-демо и расчёт метрик/ROC.

---

## Описание проекта

Система распознавания эмоций по лицу в реальном времени, разработанная для учебного CV-проекта. 
Проект реализует полный цикл машинного обучения: от подготовки данных до развертывания готового приложения.

### Основные возможности:
- **Обучение модели** на датасете FER2013 с использованием ResNet18
- **Детекция лиц** в реальном времени через Haar Cascade
- **Классификация эмоций**: 7 классов (angry, disgust, fear, happy, neutral, sad, surprise)
- **Веб-интерфейс** на Streamlit с поддержкой камеры
- **Расширенная аналитика**: ROC-кривые, Confusion Matrix, F1-score

### Классы эмоций:
- `angry` - злость
- `disgust` - отвращение  
- `fear` - страх
- `happy` - счастье
- `neutral` - нейтральное
- `sad` - грусть
- `surprise` - удивление

---

## Архитектура и документация

### Дизайн-проект:
- **[Блок-схема алгоритма](docs/block_diagram.md)** - Подробное описание этапов обучения и инференса
- **[Схема взаимодействия компонентов](docs/architecture.md)** - Архитектура системы и потоки данных
- **[Отчет о соответствии](docs/compliance_report.md)** - Полный анализ выполнения требований
- **[Тестирование моделей](docs/testing_different_models.md)** - Сравнение архитектур и выбор лучшей модели

### Структура проекта:

```
CV-Project/
├── 📁 src/                      # Основные скрипты
│   ├── train_model.py          # Обучение модели
│   ├── camera_inference.py     # Инференс через камеру
│   ├── streamlit_app.py        # Веб-интерфейс
│   ├── prepare_dataset.py      # Подготовка датасета
│   ├── split_dataset.py        # Разделение датасета
│   └── performance_test.py     # Тест производительности
├── 📁 utils/                    # Утилиты
│   └── metrics.py              # Расчет метрик
├── 📁 scripts/                  # Вспомогательные скрипты
│   └── camera_test.py          # Тест камеры
├── 📁 docs/                     # Документация
│   ├── block_diagram.md        # Блок-схемы
│   ├── architecture.md         # Архитектура
│   ├── compliance_report.md    # Отчет о соответствии
│   ├── tech-statement.md       # Техническое описание
│   └── testing_different_models.md  # Тестирование моделей
├── 📁 train/                    # Обучающие данные
├── 📁 test/                     # Тестовые данные  
├── 📁 env/                      # Python окружение
├── 📁 logs/                     # Логи обучения
├── 📁 metrics/                  # Метрики и графики
├── 📄 start.sh                 # Скрипт быстрого запуска
├── 📄 requirements.txt         # Зависимости
└── 📄 README.md                # Документация
```

### Структура данных:

```
train/
├── angry/
├── disgust/
├── fear/
├── happy/
├── neutral/
├── sad/
└── surprise/
test/
└── ... (те же классы)
```

Подготовка датасета: `prepare_dataset.py` (если нужно) и `split_dataset.py`.

---

## Быстрый старт

### 1. Активация окружения:
```bash
source env/bin/activate
```

### 2. Установка зависимостей:
```bash
pip install -r requirements.txt
```

### 3. Запуск обучения:
```bash
# Быстрый режим (2 эпохи, img_size=128)
./start.sh

# Полный режим (8 эпох, img_size=224)  
FULL=1 ./start.sh
```

### 4. Запуск приложения:
```bash
# Веб-интерфейс (рекомендуется)
streamlit run src/streamlit_app.py

# Или камера через OpenCV
python src/camera_inference.py
```

---

## Подробное описание модулей

### train_model.py
Основной скрипт обучения модели ResNet18:
- **Аугментация**: RandomResizedCrop, RandomHorizontalFlip
- **Transfer Learning**: Заморозка слоев, дообучение layer4
- **Оптимизация**: AdamW с weight decay, CosineAnnealingLR
- **Метрики**: Accuracy, F1, Confusion Matrix, ROC AUC

### camera_inference.py  
Работа с камерой через OpenCV:
- Детекция лиц Haar Cascade
- Предсказание в реальном времени
- Наложение результатов на кадр

### streamlit_app.py
Веб-интерфейс с расширенным функционалом:
- **Live камера** с детекцией лиц
- **Загрузка фото** для анализа
- **Метрики** с визуализацией
- **Настройки** параметров детекции

### metrics.py
Утилиты для оценки качества:
- Расчет всех метрик
- Построение графиков
- Сохранение результатов

---

## Метрики качества

### Реализованные метрики:
- **Accuracy** - Точность классификации
- **F1 Score** - Гармоническое среднее precision/recall  
- **Confusion Matrix** - Матрица ошибок
- **ROC Curve** - ROC-кривые для каждого класса
- **ROC AUC** - Площадь под ROC-кривой

### Оптимизация модели:
- **Аугментация данных** для улучшения обобщения
- **Transfer Learning** с предобученным ResNet18
- **Class weights** для балансировки датасета
- **Fine-tuning** последних слоев

---

## Параметры окружения

- `MODEL_PATH` — путь к весам модели (по умолчанию `wafflelover404_emotion_model.pth`)
- `IMG_SIZE` — размер входного изображения (по умолчанию `128` в fast, `224` в full)
- `BATCH_SIZE`, `EPOCHS`, `LR`, `WEIGHT_DECAY`, `UNFREEZE` — параметры обучения
- `FAST` — включить быстрый режим (1/0)

### Режимы обучения:
- **Fast mode**: `FAST=1` - 2 эпохи, img_size=128, batch_size=128
- **Full mode**: `FAST=0` - 8 эпох, img_size=224, batch_size=32

---

## Соответствие требованиям

### Критерий 1: Дизайн-проект
- **Общее описание**: Полная документация в README.md
- **Блок-схема**: Подробные схемы в [block_diagram.md](docs/block_diagram.md)
- **Схема взаимодействия**: Архитектура в [architecture.md](docs/architecture.md)

### Критерий 2: Прототип 
- **Базовый функционал**: Обучение ResNet18, классификация фото
- **Соответствие дизайн-проекту**: Полная реализация
- **Запуск на целевом устройстве**: camera_inference.py + streamlit_app.py
- **Время обработки ≤2с**: Оптимизированная модель
- **Accuracy**: Реализован расчет на тестовых данных
- **ROC-кривая**: Построение для каждого класса

### Критерий 3: Оптимизация 
- **Аугментация**: RandomResizedCrop, RandomHorizontalFlip
- **Дообучение**: Transfer Learning, fine-tuning layer4
- **Метрики на тесте**: Accuracy, F1, Confusion Matrix, ROC
- **Confusion Matrix**: Визуализация и сохранение
- **F1 метрика**: Macro-average расчет

### Критерий 4: Сборка и запуск 
- **Корректный запуск**: Streamlit + OpenCV интерфейсы
- **Отсутствие падений**: Обработка ошибок
- **Наложение результатов**: Bounding boxes с эмоциями
- **Вывод уверенности**: Вероятности для каждого класса
- **Демо на камере**: Real-time детекция

---

## Использование

### 1) Камера (OpenCV)
```bash
source env/bin/activate
python src/camera_inference.py
```
**Особенности:**
- Детекция лиц в реальном времени
- Наложение предсказаний на кадр
- Вывод уверенности модели

### 2) Streamlit UI (рекомендуется)
```bash
source env/bin/activate
streamlit run src/streamlit_app.py
```
**Функционал интерфейса:**
- **Камера** - Live-демо с детекцией лиц
- **Загрузка фото** - Анализ загруженных изображений  
- **Метрики и ROC** - Визуализация результатов
- **Настройки** - Параметры детекции и модели

---

## Требования

### requirements.txt:
```
torch>=1.12.0
torchvision>=0.13.0
opencv-python>=4.5.0
streamlit>=1.28.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
seaborn>=0.11.0
numpy>=1.21.0
Pillow>=8.3.0
```

### Установка:
```bash
source env/bin/activate
pip install -r requirements.txt
```

---

## Полезные скрипты

- `scripts/camera_test.py` — Проверка работы вебкамеры
- `utils/metrics.py` — Расчет и визуализация метрик  
- `src/split_dataset.py` — Разделение датасета на train/test
- `src/performance_test.py` — Тест производительности
- `start.sh` — Скрипт быстрого запуска обучения

---

## Docker (опционально)

### Dockerfile:
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8501
CMD ["streamlit", "run", "src/streamlit_app.py"]
```

### Сборка и запуск:
```bash
docker build -t emotion-demo .
docker run -p 8501:8501 emotion-demo
```

---

## Результаты работы

### Файлы результатов:
- **Модели**: `wafflelover404_emotion_model.pth`, `wafflelover404_emotion_model_best.pth`
- **Метрики**: папка `metrics/`
  - `accuracy.txt` - точность модели
  - `f1_score.txt` - F1 метрика
  - `confusion_matrix.txt` - матрица ошибок
  - `roc_auc.json` - ROC AUC по классам
  - `roc_curve.png` - ROC кривые
  - `classification_report.txt` - подробный отчет

### Логи:
- `logs/train.log` - логи обучения с метриками по эпохам

---

## Автор и лицензия

**Учебный проект** по компьютерному зрению.  
Использует ResNet18 (PyTorch) и Haar Cascade (OpenCV) для детекции лиц.

**Технологический стек:**
- Python 3.9+
- PyTorch + Torchvision
- OpenCV
- Streamlit
- scikit-learn

---

## Поддержка

При возникновении проблем:
1. Проверьте установку зависимостей: `pip install -r requirements.txt`
2. Убедитесь в наличии датасета в папках `train/` и `test/`
3. Проверьте работу камеры: `python scripts/camera_test.py`
4. Протестируйте производительность: `python src/performance_test.py`
5. Изучите логи в `logs/train.log`

